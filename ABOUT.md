# 排序算法全面解析：原理、分类与应用

## 目录
- 排序算法简介
- 常见排序算法
  - 1. 冒泡排序
  - 2. 选择排序
  - 3. 插入排序
  - 4. 快速排序
  - 5. 归并排序
  - 6. 堆排序
  - 7. 希尔排序
  - 8. 计数排序
  - 9. 桶排序
  - 10. 基数排序
- 算法对比
- 总结

---

## 排序算法简介
排序是将一组数据按照特定顺序（升序/降序）重新排列的过程，是计算机科学中最基础的算法类型之一。根据实现原理可分为：
- **比较排序**：通过元素比较决定顺序（如快速排序、归并排序）
- **非比较排序**：利用元素特征进行排序（如计数排序、基数排序）
- **稳定排序**：相等元素相对位置不变
- **不稳定排序**：相等元素可能改变相对位置

---

## 常见排序算法

### 1. 冒泡排序
**原理**：  
通过相邻元素比较交换，使最大/小元素逐渐"浮"到数列末端

**步骤**：
1. 比较相邻元素，前大后小则交换
2. 对每一对相邻元素重复操作
3. 重复上述步骤直到排序完成

**复杂度**：
- 时间复杂度：O(n²)（最坏/平均） / O(n)（最好，已排序时）
- 空间复杂度：O(1)

**特点**：  
实现简单但效率低，适合小规模数据教学演示

---

### 2. 选择排序
**原理**：  
每次从未排序部分选择最小/大元素放到已排序序列末尾

**步骤**：
1. 在未排序序列中找到最小元素
2. 与未排序序列首元素交换位置
3. 重复上述步骤直到排序完成

**复杂度**：
- 时间复杂度：O(n²)
- 空间复杂度：O(1)

**特点**：  
交换次数少（O(n)次），适合对交换成本敏感的场景

---

### 3. 插入排序
**原理**：  
将未排序元素逐个插入已排序序列的合适位置

**步骤**：
1. 从第一个元素开始视为已排序
2. 取出下一个元素，在已排序序列中从后向前扫描
3. 找到合适位置插入
4. 重复步骤2-3直到完成

**复杂度**：
- 时间复杂度：O(n²) / O(n)（已排序时）
- 空间复杂度：O(1)

**特点**：  
对小规模/基本有序数据效率高，常作为高级算法的子过程

---

### 4. 快速排序
**原理**：  
采用分治策略，选取基准元素将数组分为两个子数组

**步骤**：
1. 选择基准元素（pivot）
2. 分区操作：小于基准的放左边，大于的放右边
3. 递归地对左右子数组排序

**复杂度**：
- 时间复杂度：O(n log n)（平均） / O(n²)（最坏）
- 空间复杂度：O(log n)

**特点**：  
实际应用中最快的通用排序算法，需注意基准选择策略

---

### 5. 归并排序
**原理**：  
典型分治算法，将数组分解合并时排序

**步骤**：
1. 递归地将数组分成两半
2. 对每半进行排序
3. 合并两个已排序的半边数组

**复杂度**：
- 时间复杂度：O(n log n)
- 空间复杂度：O(n)

**特点**：  
稳定排序，适合链表结构，外部排序常用方案

---

### 6. 堆排序
**原理**：  
利用堆数据结构进行选择排序

**步骤**：
1. 构建最大堆/最小堆
2. 将堆顶元素与末尾元素交换
3. 调整堆结构，重复步骤2直到排序完成

**复杂度**：
- 时间复杂度：O(n log n)
- 空间复杂度：O(1)

**特点**：  
原地排序，适合需要同时进行插入和取极值的场景

---

### 7. 希尔排序
**原理**：  
改进的插入排序，通过分组增量进行预处理

**步骤**：
1. 按增量序列分组
2. 对各组进行插入排序
3. 逐渐缩小增量重复操作

**复杂度**：
- 时间复杂度：O(n log² n)（最优）
- 空间复杂度：O(1)

**特点**：  
中等规模数据表现良好，增量序列选择影响性能

---

### 8. 计数排序
**原理**：  
统计元素出现次数实现排序

**步骤**：
1. 统计每个元素的出现次数
2. 计算元素的位置前缀和
3. 反向填充目标数组

**复杂度**：
- 时间复杂度：O(n + k)（k为数值范围）
- 空间复杂度：O(k)

**特点**：  
非比较排序，适合数据范围较小的整数排序

---

### 9. 桶排序
**原理**：  
将数据分到有限数量的桶里，每个桶单独排序

**步骤**：
1. 设置固定数量的空桶
2. 将数据分配到对应桶中
3. 对每个非空桶排序
4. 合并所有桶中数据

**复杂度**：
- 时间复杂度：O(n + k)（k为桶数量）
- 空间复杂度：O(n + k)

**特点**：  
适合均匀分布的数据，可与其它排序算法配合使用

---

### 10. 基数排序
**原理**：  
按位数逐位排序，从最低位到最高位

**步骤**：
1. 取得数组中的最大数确定位数
2. 从低位开始进行稳定排序（通常用计数排序）
3. 重复直到最高位排序完成

**复杂度**：
- 时间复杂度：O(nk)（k为最大位数）
- 空间复杂度：O(n + k)

**特点**：  
适合整数和定长字符串排序，需要稳定子排序算法

---

## 算法对比
| 算法         | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 |
|--------------|----------------|----------------|------------|--------|
| 冒泡排序     | O(n²)          | O(n²)          | O(1)       | 稳定   |
| 选择排序     | O(n²)          | O(n²)          | O(1)       | 不稳定 |
| 插入排序     | O(n²)          | O(n²)          | O(1)       | 稳定   |
| 快速排序     | O(n log n)     | O(n²)          | O(log n)   | 不稳定 |
| 归并排序     | O(n log n)     | O(n log n)     | O(n)       | 稳定   |
| 堆排序       | O(n log n)     | O(n log n)     | O(1)       | 不稳定 |
| 希尔排序     | O(n log² n)    | O(n²)          | O(1)       | 不稳定 |
| 计数排序     | O(n + k)       | O(n + k)       | O(k)       | 稳定   |
| 桶排序       | O(n + k)       | O(n²)          | O(n + k)   | 稳定   |
| 基数排序     | O(nk)          | O(nk)          | O(n + k)   | 稳定   |

---

## 总结
选择排序算法时应综合考虑：
1. 数据规模
2. 数据分布特征
3. 内存限制
4. 稳定性需求
5. 实现复杂度

通用场景推荐快速排序，需要稳定时选择归并排序，对整数排序可考虑计数/基数排序。理解各算法原理有助于在实际应用中做出最优选择。
